imports:
    - { resource: config.yml }

#framework:
#    validation:
#        cache: validator.mapping.cache.doctrine.apc
#    serializer:
#        cache: serializer.mapping.cache.doctrine.apc

#doctrine:
#    orm:
#        metadata_cache_driver: apc
#        result_cache_driver: apc
#        query_cache_driver: apc

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: notice
            handler:      nested

        nested:
            type: syslogudp
            host: '%monolog.handler.syslogudp.host%'
            port: '%monolog.handler.syslogudp.port%'

        console:
            type:  console
